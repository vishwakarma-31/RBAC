{
  "version": 2,
  "buildCommand": "npm install && npm run build -w @rbac-platform/shared && npm run build -w @rbac-platform/admin-dashboard",
  "outputDirectory": "dashboard/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
